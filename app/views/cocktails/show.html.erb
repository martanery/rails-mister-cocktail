<h2><%= @cocktail.name %></h2>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li>
      <%= dose.ingredient.name %>
      <%= dose.description %>
    </li>
  <% end %>
</ul>
<%= link_to "Cocktails", cocktails_path %>
<%= link_to "CreateDose", new_cocktail_dose_path(@cocktail) %>
